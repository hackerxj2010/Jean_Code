[package]
name = "pi-shell"
description = "Embedded bash (brush fork) with sessions that survive across calls"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
pi-builtins = { path = "../pi-builtins" }
