[package]
name = "pi-iso"
description = "Task isolation: copy-on-write views (APFS clonefile, btrfs/XFS reflink, ReFS block cloning, or copies) and three-way merge back"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
pi-builtins = { path = "../pi-builtins" }
