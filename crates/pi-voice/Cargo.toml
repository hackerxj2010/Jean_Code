[package]
name = "pi-voice"
description = "Audio decoding (WAV, FLAC, AIFF, .au, G.711; the rest through ffmpeg), resampling, turn detection, and microphone capture"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
