[package]
name = "pi-ffi"
description = "The Rust core as an in-process library, for callers that cannot wait on a process start"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
name = "jean_native"
crate-type = ["cdylib", "rlib"]

[dependencies]
pi-natives = { path = "../pi-natives" }
